{
  "name": "clutterdock",
  "productName": "ClutterDock",
  "version": "1.2.1",
  "description": "Free app folders for your Windows taskbar / tray — group apps, files, folders, and URLs.",
  "main": "src/main.js",
  "author": "Ronald",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/rbakman-rgb/clutterdock.git"
  },
  "homepage": "https://clutterdock.com/",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node scripts/test-license.js && node scripts/test-store.js && node scripts/test-url-scheme.js && node scripts/test-diagnostics.js",
    "pack": "electron-builder --dir",
    "predist": "node scripts/write-license-secret.js --require && npm test",
    "dist": "electron-builder --win --publish never",
    "predist:portable": "node scripts/write-license-secret.js --require && npm test",
    "dist:portable": "electron-builder --win portable --publish never"
  },
  "build": {
    "appId": "com.ronald.ClutterDock",
    "productName": "ClutterDock",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "rbakman-rgb",
        "repo": "clutterdock"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.png",
      "artifactName": "ClutterDock-${version}-${arch}.${ext}"
    },
    "protocols": [
      {
        "name": "ClutterDock",
        "schemes": ["clutterdock"]
      }
    ],
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "ClutterDock"
    }
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8"
  }
}
