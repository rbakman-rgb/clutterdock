# ClutterDock launcher — 10 UI/UX directions (RON-488)

Interactive mockups for review. **Not shipped** to clutterdock.com.

```bash
# from repo root
python3 -m http.server 8765 --directory docs/design-options
# then open http://localhost:8765
```

Pick a direction (or mix) before implementing in the Mac/Windows apps.
